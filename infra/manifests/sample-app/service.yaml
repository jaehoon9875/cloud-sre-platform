apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: apps
  labels:
    app.kubernetes.io/name: sample-app
spec:
  selector:
    app.kubernetes.io/name: sample-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
