apiVersion: v1
kind: Service
metadata:
  name: gke-spot-exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: gke-spot-exporter
spec:
  selector:
    app.kubernetes.io/name: gke-spot-exporter
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
